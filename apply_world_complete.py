import sys

with open('/workspaces/nutricare-frontend/app.html', 'r', encoding='utf-8') as f:
    content = f.read()

old_advance = "    currentCase += 1;\n    if (currentCase > 12) {\n      var finishedMonde = currentMonde;\n      var finishedChemin = profile.parcours_chemin;\n      currentCase = 0;\n      currentMonde += 1;\n      var _prevWorldKey = profile.parcours_world_key || 'foret_eveil';\n      profile.parcours_world_key = profile.parcours_chemin ? pcNextWorldKey(_prevWorldKey, profile.parcours_chemin) : _prevWorldKey;\n      profile.parcours_monde_theme = profile.parcours_chemin || null;\n      profile.parcours_chemin = null;\n      if (localStorage.getItem(pcSeenKey('fin', finishedMonde)) !== '1') {\n        localStorage.setItem(pcSeenKey('fin', finishedMonde), '1');\n        var _finSequence;\n        if (finishedMonde === 2 && finishedChemin === 'A') {\n          _finSequence = [PC_VIDEO_BASE + 'pc_choice_a_monde2_partie2.mp4', PC_VIDEO_BASE + 'pc_choice_a_monde2_partie3.mp4'];\n        } else if (finishedMonde === 2 && finishedChemin === 'B') {\n          _finSequence = [PC_VIDEO_BASE + 'pc_choice_b_monde2_partie2.mp4'];\n        } else {\n          _finSequence = _pcVideoMap.fin(finishedMonde);\n        }\n        setTimeout(function() { pcPlaySequence(Array.isArray(_finSequence) ? _finSequence : [_finSequence]); }, 300);\n      }\n    }\n    if (currentCase === _pcForkCase) { profile.parcours_chemin = null; }"
new_advance = '    currentCase += 1;\n    if (currentCase > 12) { currentCase = 12; }\n    if (currentCase === _pcForkCase) { profile.parcours_chemin = null; }'

if old_advance not in content:
    print('ERREUR: ancre advanceParcoursIfNeeded introuvable')
    sys.exit(1)
content = content.replace(old_advance, new_advance, 1)

js_addition = "  window.pcCompleteWorld = function() {\n    var modal = document.getElementById('worldCompleteModal');\n    if (modal) modal.style.display = 'none';\n    var profile = JSON.parse(localStorage.getItem('nutricare_profile') || '{}');\n    var finishedMonde = profile.parcours_monde || 1;\n    var finishedChemin = profile.parcours_chemin;\n    var _finSequence;\n    if (finishedMonde === 2 && finishedChemin === 'A') {\n      _finSequence = [PC_VIDEO_BASE + 'pc_choice_a_monde2_partie2.mp4', PC_VIDEO_BASE + 'pc_choice_a_monde2_partie3.mp4'];\n    } else if (finishedMonde === 2 && finishedChemin === 'B') {\n      _finSequence = [PC_VIDEO_BASE + 'pc_choice_b_monde2_partie2.mp4'];\n    } else {\n      _finSequence = _pcVideoMap.fin(finishedMonde);\n    }\n    pcPlaySequence(Array.isArray(_finSequence) ? _finSequence : [_finSequence], function() {\n      var p = JSON.parse(localStorage.getItem('nutricare_profile') || '{}');\n      var _prevWorldKey = p.parcours_world_key || 'foret_eveil';\n      p.parcours_world_key = p.parcours_chemin ? pcNextWorldKey(_prevWorldKey, p.parcours_chemin) : _prevWorldKey;\n      p.parcours_monde_theme = p.parcours_chemin || null;\n      p.parcours_chemin = null;\n      p.parcours_case = 0;\n      p.parcours_monde = (p.parcours_monde || 1) + 1;\n      localStorage.setItem('nutricare_profile', JSON.stringify(p));\n      try {\n        var token = authToken || window._authToken || localStorage.getItem('nutricare_token');\n        if (token) {\n          fetch(BACKEND_URL + '/api/profile', {\n            method: 'PUT',\n            headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token },\n            body: JSON.stringify({ parcours_case: p.parcours_case, parcours_monde: p.parcours_monde, parcours_world_key: p.parcours_world_key, parcours_monde_theme: p.parcours_monde_theme, parcours_chemin: p.parcours_chemin })\n          });\n        }\n      } catch(e) {}\n      renderParcours(p);\n    });\n  };\n  function saveParcoursChoice(choice) {"
old_anchor_js = '  function saveParcoursChoice(choice) {'
idx_js = content.find(old_anchor_js)
if idx_js == -1:
    print('ERREUR: ancre saveParcoursChoice introuvable')
    sys.exit(1)
content = content[:idx_js] + js_addition + content[idx_js + len(old_anchor_js):]

old_trigger_anchor = "    if (caseIdx >= _pcForkCase && !profile.parcours_chemin && window._currentView === 'home') {"
trigger_addition = "    if (caseIdx === 12 && window._currentView === 'home') {\n      var _wcModal = document.getElementById('worldCompleteModal');\n      if (_wcModal && _wcModal.style.display !== 'flex') {\n        _wcModal.style.display = 'flex';\n        setTimeout(function() { if (typeof pcLaunchConfetti === 'function') pcLaunchConfetti('worldCompleteConfetti', 4500); }, 150);\n      }\n    }\n    if (caseIdx >= _pcForkCase && !profile.parcours_chemin && window._currentView === 'home') {"
if old_trigger_anchor not in content:
    print('ERREUR: ancre trigger introuvable')
    sys.exit(1)
content = content.replace(old_trigger_anchor, trigger_addition, 1)

modal_html = '<div id="worldCompleteModal" style="display:none;position:fixed;inset:0;background:rgba(20,10,8,0.72);z-index:99999;align-items:center;justify-content:center;padding:20px">\n          <div style="width:100%;max-width:340px;background:#FFFCF8;border-radius:32px;overflow:hidden;box-shadow:0 30px 70px rgba(0,0,0,0.4);position:relative">\n            <div style="background:linear-gradient(150deg,#FF6B6B,#FF9A3C 50%,#FFD166);padding:30px 20px 38px;text-align:center;position:relative;overflow:hidden">\n              <canvas id="worldCompleteConfetti" style="position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:5"></canvas>\n              <div style="display:inline-flex;align-items:center;gap:5px;background:rgba(255,255,255,0.22);color:#fff;font-size:0.62rem;font-weight:800;padding:5px 14px;border-radius:20px;letter-spacing:0.08em;text-transform:uppercase;margin-bottom:22px;position:relative;z-index:2">\n                <i class="ti ti-trophy" style="font-size:13px"></i>Monde terminé\n              </div>\n              <div style="position:relative;width:104px;height:104px;margin:0 auto 20px;z-index:2">\n                <div style="position:absolute;inset:-6px;border-radius:32px;background:linear-gradient(135deg,#FFD166,#FFF7E0,#FFD166);opacity:0.9"></div>\n                <div style="position:relative;width:104px;height:104px;border-radius:28px;overflow:hidden;box-shadow:0 10px 28px rgba(0,0,0,0.2)">\n                  <video id="worldCompleteVideo" autoplay muted loop playsinline style="width:100%;height:100%;object-fit:cover;display:block">\n                    <source src="https://www.mirella-ai.fr/onboarding_success_final2.mp4" type="video/mp4">\n                  </video>\n                </div>\n              </div>\n              <div style="font-family:Open Sans,serif;font-size:1.42rem;font-weight:800;color:#fff;margin:0 0 8px;text-shadow:0 2px 12px rgba(0,0,0,0.15);position:relative;z-index:2">Bravo, vous avez terminé ce monde !</div>\n              <div style="font-size:0.85rem;color:rgba(255,255,255,0.94);line-height:1.55;margin:0;max-width:260px;margin:0 auto;position:relative;z-index:2">L\'aventure continue, un nouveau monde n\'attend que vous.</div>\n            </div>\n            <div style="padding:24px 24px 26px;text-align:center">\n              <div style="display:inline-flex;align-items:center;gap:7px;background:rgba(255,209,102,0.16);border:1px solid rgba(239,159,39,0.4);color:#993C1D;font-size:0.78rem;font-weight:700;padding:8px 18px;border-radius:20px;margin-bottom:22px">\n                <i class="ti ti-circle-check-filled" style="font-size:16px;color:#EF9F27"></i>12 étapes accomplies\n              </div>\n              <button onclick="pcCompleteWorld()" style="width:100%;background:linear-gradient(135deg,#FF6B6B,#FF9A3C,#FFD166);color:#fff;border:none;border-radius:18px;padding:17px;font-size:0.98rem;font-weight:800;cursor:pointer;font-family:DM Sans,sans-serif;box-shadow:0 14px 34px rgba(255,107,107,0.45);letter-spacing:0.2px">Continuer l\'aventure</button>\n            </div>\n          </div>\n        </div>\n        <div id="parcoursForkModal"'
old_modal_anchor = '<div id="parcoursForkModal"'
idx_modal = content.find(old_modal_anchor)
if idx_modal == -1:
    print('ERREUR: ancre modal HTML introuvable')
    sys.exit(1)
content = content[:idx_modal] + modal_html + content[idx_modal + len(old_modal_anchor):]

with open('/workspaces/nutricare-frontend/app.html', 'w', encoding='utf-8') as f:
    f.write(content)

print('OK: popup fin de monde ajoutee, avec confettis et bouton Continuer l aventure')
